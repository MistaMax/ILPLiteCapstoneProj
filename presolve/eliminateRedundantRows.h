#pragma once
#include <Eigen/Dense>
#include <string>
#include <iostream>

using namespace Eigen;
using namespace std;

int eliminateRedundantRows(MatrixXd *A, VectorXd *b, VectorXd *A_eq)
{
	return 0;
}